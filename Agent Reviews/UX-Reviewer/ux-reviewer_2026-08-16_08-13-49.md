# UX Reviewer — Report
**Run date/time:** 2026-08-16 08:13:49 (local)
**Target:** https://tdev.onenessyoga.in (Contabo VM, dev environment, first live-HTTPS deployment)

## IMPORTANT LIMITATION — READ FIRST

This session's tool set did **not** include a browser automation tool (no Playwright/Chrome DevTools MCP was available — only file-system Read/Glob/Grep, Bash/curl, and Write). The requested test plan explicitly requires:

- Navigating pages and taking screenshots
- Logging in as three different roles through the UI
- Reading the real browser console for errors/warnings
- Clicking buttons, submitting forms, opening modals (leave application, sequence assignment, resource folder create/delete, sequence detail view)
- Triggering the native "Enable Notifications" permission prompt
- Checking the browser's PWA install affordance (installability banner/omnibox icon)
- Testing responsive layout at 390px/1280px visually

**None of this can be done with curl.** I did not fabricate results for these items. Instead, I ran what I could verify at the HTTP/network level to catch the specific class of issues called out as priority ("wrong API URLs, CORS issues, missing assets, mixed-content warnings, etc."). Everything below is either (a) an actual verified finding, or (b) explicitly marked NOT TESTED.

**Recommendation:** re-run this review with a Playwright (or Chrome DevTools) MCP browser tool enabled so the full role-based UI walkthrough, console-log capture, and screenshots can be performed against https://tdev.onenessyoga.in.

---

## What was verified (via curl / HTTP)

| Check | Result |
|---|---|
| Root `/` loads | HTTP 200, `text/html`, nginx 1.28.3, valid HTTPS |
| `http://` → `https://` redirect | HTTP 301, redirects to `https://tdev.onenessyoga.in/` — no mixed-content risk from the bare domain |
| index.html asset references | `<script src="/assets/index-DYXU2tHi.js">`, `<link href="/assets/index-ClK7-U1u.css">` — both same-origin, absolute paths, no localhost/stale hashes |
| `manifest.webmanifest` | Present at `/manifest.webmanifest`, HTTP 200, valid JSON: name "Oneness Yoga", `start_url: /`, `display: standalone`, icons (192/512 SVG, incl. maskable) all resolve |
| `manifest.json` (wrong/legacy path) | Returns the SPA's `index.html` fallback (200, `text/html`) rather than a 404 — harmless since the `<link rel="manifest">` correctly points at `/manifest.webmanifest`, but see note below |
| Service worker `/sw.js` | HTTP 200, Workbox-generated (`workbox:core:7.4.0`, precaching modules present) |
| `registerSW.js` | Present, registers `/sw.js` with `scope: '/'` on `window load` — standard vite-plugin-pwa output |
| PWA icons `pwa-192x192.svg`, `pwa-512x512.svg` | Both HTTP 200 |
| JS bundle scanned for hardcoded `http://` / `localhost` API refs | Only match is axios's internal browser-env fallback string (`window.location.href||"http://localhost"`), not a real API call — **no hardcoded dev API URL leaked into the production bundle** |
| Axios `baseURL` in bundle | `"/api"` — relative/same-origin, correct for this deployment |
| CORS preflight (`OPTIONS /api/auth/login`, Origin: tdev.onenessyoga.in) | HTTP 204, `Access-Control-Allow-Origin: https://tdev.onenessyoga.in` returned correctly |
| Live login API call (Super Admin creds) | HTTP 200, valid JWT + user object returned — confirms DB/auth wiring works end-to-end on the deployed backend |
| SPA client-side routes (`/admin/dashboard`, `/trainer/sessions`, unknown deep route) | All return HTTP 200 with the SPA shell (correct nginx `try_files` fallback for client-side routing) — a hard refresh on any deep link will not 404 |
| Security headers | CSP, HSTS (`max-age=31536000; includeSubDomains`), X-Frame-Options, X-Content-Type-Options, Referrer-Policy all present and reasonable |
| Rate limiting | `RateLimit-Policy: 10;w=900` observed on the login endpoint — sensible brute-force protection |

No deployment-level breakage was found in any of the above: no mixed content, no CORS failures, no stale/wrong asset hashes, no hardcoded localhost API URLs, and the SPA fallback routing is configured correctly for deep links.

## Findings table

| Page/Area | Severity | Issue | Suggested fix |
|---|---|---|---|
| Deployment (`/manifest.json`) | LOW | Requesting `/manifest.json` (a conventional/legacy path some tools and crawlers still probe for) silently returns the SPA's `index.html` with `text/html` instead of a 404 or the real manifest. Not harmful today since the actual `<link rel="manifest">` tag correctly points at `/manifest.webmanifest`, but any external tool/lint that specifically checks `/manifest.json` will get a false "not a valid manifest" result. | No action required unless a specific integration expects `manifest.json`; if so, add an nginx alias from `/manifest.json` to `/manifest.webmanifest`. |
| All roles / all pages | INFO / NOT TESTED | Full UI walkthrough (login flow for all 3 roles, Dashboard/Trainers/Sessions/Sequences/Leaves/Resources per role, search/filter bar, resource folder create+delete, sequence detail read-only view, leave application, sequence assignment to trainer, "Enable Notifications" permission prompt, PWA install banner, browser console errors/warnings, responsive layout at 390px/1280px, accessibility basics) | Requires a browser automation tool (Playwright/Chrome DevTools MCP). Re-run this review with that tool enabled against https://tdev.onenessyoga.in. |

## Suggested next step

Since curl-level checks came back clean, the deployment plumbing (nginx, TLS, CORS, asset paths, SPA routing, service worker/manifest serving, backend auth) appears sound. The remaining, more valuable checks — actual UI rendering, console errors, the notification permission prompt, and PWA installability as perceived by the browser — need a real browser session. Suggest re-invoking this agent in an environment with a Playwright/browser MCP tool attached, or having a human manually spot-check the three login flows and the "Enable Notifications" button once.
