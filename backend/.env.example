PORT=3000
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Generate these with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:admin@yourdomain.com

DB_PATH=./data/oneness.db
